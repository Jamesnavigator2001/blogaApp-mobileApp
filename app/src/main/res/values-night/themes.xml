<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/deepSkyBlue</item>
        <item name="colorPrimaryVariant">@color/deepSkyBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/deepSkyBlue</item>
        <item name="colorSecondary">@color/deepSkyBlue</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/deepSkyBlue</item>
        <item name="android:colorBackground">@color/white</item> <!-- Use android:colorBackground -->
    </style>

    <style name="Theme.BlogApp" parent="Base.Theme.BlogApp" />
</resources>
